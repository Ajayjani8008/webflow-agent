# Pending Designer Work — <site-id>

Work that cannot be completed through the API and needs a human in the Webflow Designer.
**Scope: THIS SITE ONLY.** Items from another site never block this site's "complete" (they live in that site's file).

Format: `- [ ] [source-skill] [YYYY-MM-DD] [page — section] — exact steps, exact values, why the API can't do it`
Priority tags: `[critical]` (a missing feature — animations, bindings, module init) · `[optional]` (polish/review) · `[blocked]` (waiting on the user for assets/decisions)

A section is never reported "complete" while it owns an open `[critical]` item here. Closed items move to `## Done` with the date they were verified in the Designer — never deleted, they are the audit trail.

---

## Open

## Done
